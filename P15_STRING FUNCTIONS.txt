INPUT:
#include <stdio.h>
#include <string.h>
int main()
{
   char arr[]="swara";
   int len1=strlen(arr);
   printf ("string=%s length=%d\n",arr,len1);
   return 0;
}

OUTPUT:
string=swara length=5